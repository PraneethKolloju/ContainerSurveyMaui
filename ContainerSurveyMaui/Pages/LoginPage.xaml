<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ContainerSurveyMaui.Pages.LoginPage"
             Shell.NavBarIsVisible="False">
    <ScrollView>
        <VerticalStackLayout HeightRequest="5">
            <ActivityIndicator IsRunning="false"
       Color="#005398" />
            <!--<Grid>
            <StackLayout Margin="30">
                <Image Source="vedanta_logo.png" HeightRequest="150"  HorizontalOptions="CenterAndExpand"/>
                <StackLayout Margin="0,18" HorizontalOptions="CenterAndExpand" >
                    <Label Style="{StaticResource welcome}" FontSize="25" Text="Welcome" HorizontalOptions="Center" />
                    <Label FontFamily="OpenSansRegular" TextColor="{StaticResource SecondaryColor}" 
                       FontSize="15" Text="Container Survey Application"/>
                </StackLayout>

                <StackLayout>
                    <Label FontFamily="OpenSansRegular" TextColor="{StaticResource SecondaryColor}" 
                       FontSize="16" Text="Username"/>
                    <Grid Margin="0,5">
                        <Frame Opacity="0.3" BackgroundColor="White" CornerRadius="5"/>
                        <Entry x:Name="userNameEntry" BackgroundColor="Transparent" Placeholder="Enter email" FontSize="15" 
                           TextColor="#000000" Margin="5,0" 
                           PlaceholderColor="Gray" />

                    </Grid>

                    <Label FontFamily="OpenSansRegular" TextColor="{StaticResource SecondaryColor}" 
                       FontSize="16" Text="Password"/>
                    <Grid Margin="0,5">
                        <Frame Opacity="0.3" BackgroundColor="White" CornerRadius="5"/>
                        <Entry x:Name="passwordEntry" BackgroundColor="Transparent" Placeholder="Enter password" FontSize="15"
                           TextColor="#000000" Margin="5,0"
                           PlaceholderColor="Gray" IsPassword="True"/>
                    </Grid>
                </StackLayout>

                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Button Margin="0,15" BackgroundColor="#005298"
                Text="Sign in" TextColor="{StaticResource PrimaryColor}" FontFamily="OpenSansBold" Clicked="Login_Clicked"/>
                    <ImageButton Margin="20,0" Source="touch" HeightRequest="30" WidthRequest="30"  Clicked="Bio_Clicked" Grid.Column="1"/>

                </Grid>


                <StackLayout VerticalOptions="FillAndExpand" Orientation="Horizontal">

                    <Label FontFamily="OpenSansRegular" TextColor="#000000" 
                       FontSize="16" Text="Forgot Password?" VerticalOptions="EndAndExpand" HorizontalOptions="StartAndExpand" 
                       />
                    <Label FontFamily="OpenSansRegular" TextColor="#000000" 
                       FontSize="16" Text="Register"  VerticalOptions="EndAndExpand" 
                       HorizontalOptions="EndAndExpand" 
                       />
                </StackLayout>
            </StackLayout>
        </Grid>-->
            <Grid HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" >
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <Image Grid.Row="0" Margin="0,-30,0,0" Source="vedanta_logo" HeightRequest="200" WidthRequest="300"></Image>
                <Frame Grid.Row="1"  Margin="10,-40,10,10" BorderColor="Black" BackgroundColor="White" HasShadow="False"  CornerRadius="25">
                    <Grid HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" RowSpacing="20" Padding="20,50,20,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition Height="Auto"></RowDefinition>
                        </Grid.RowDefinitions>
                        <Label Grid.Row="0"  Text="Welcome!" FontSize="24" FontAttributes="Bold" TextColor="#005298" HorizontalOptions="CenterAndExpand"></Label>
                        <Label Grid.Row="1" FontFamily="GeoramaLight" Margin="0,-15,0,0" Text="Container Survey Application" TextColor="#005298"  HorizontalOptions="CenterAndExpand"></Label>
                        <Entry Grid.Row="2" x:Name="userNameEntry" Placeholder="Username" Margin="0,20,0,0" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand"></Entry>
                        <Entry Grid.Row="3" x:Name="passwordEntry" Placeholder="Password" IsPassword="True" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand"></Entry>
                        <Button Grid.Row="4" Text="Login" BackgroundColor="#005298" CornerRadius="20" Clicked="Login_Clicked"></Button>
                        <Label Grid.Row="5" Text="Forgot Password" FontAttributes="Bold" HorizontalOptions="CenterAndExpand"></Label>
                        <ImageButton Grid.Row="6" Margin="20,0" Source="touch" HeightRequest="30" WidthRequest="30"  Clicked="Bio_Clicked" Grid.Column="1"/>
                        <Label Grid.Row="7" Margin="0,-10,0,0" Text="Don't have an account? Sign up here" HorizontalOptions="CenterAndExpand"></Label>
                    </Grid>
                </Frame>
            </Grid>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>